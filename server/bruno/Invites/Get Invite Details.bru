meta {
  name: Get Invite Details
  type: http
  seq: 3
}

get {
  url: {{base_url}}/api/invites/{{invite_code}}
  body: none
  auth: inherit
}

vars:pre-request {
  invite_code: {{invite_code}}
}

settings {
  encodeUrl: true
  timeout: 0
}
