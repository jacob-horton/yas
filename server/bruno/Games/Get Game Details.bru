meta {
  name: Get Game Details
  type: http
  seq: 4
}

get {
  url: {{base_url}}/api/games/{{game_id}}
  body: none
  auth: inherit
}

vars:pre-request {
  game_id: {{game_id}}
}

settings {
  encodeUrl: true
  timeout: 0
}
