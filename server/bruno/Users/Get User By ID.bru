meta {
  name: Get User By ID
  type: http
  seq: 5
}

get {
  url: {{base_url}}/api/users/{{user_id}}
  body: none
  auth: inherit
}

vars:pre-request {
  user_id: {{user_id}}
}

settings {
  encodeUrl: true
  timeout: 0
}
