meta {
  name: Get Group Details
  type: http
  seq: 3
}

get {
  url: {{base_url}}/api/groups/{{group_id}}
  body: none
  auth: inherit
}

vars:pre-request {
  group_id: {{group_id}}
}

settings {
  encodeUrl: true
  timeout: 0
}
