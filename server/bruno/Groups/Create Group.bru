meta {
  name: Create Group
  type: http
  seq: 1
}

post {
  url: {{base_url}}/api/groups
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Family"
  }
}

script:post-response {
  bru.setEnvVar('group_id', res.body.id);
}

settings {
  encodeUrl: true
  timeout: 0
}
