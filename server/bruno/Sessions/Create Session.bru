meta {
  name: Create Session
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/sessions
  body: json
  auth: inherit
}

body:json {
  {
    "email": "jacob+test@email.com",
    "password": "password123"
  }
}

script:post-response {
  bru.setEnvVar('user_id', res.body.id);
}

settings {
  encodeUrl: true
  timeout: 0
}
